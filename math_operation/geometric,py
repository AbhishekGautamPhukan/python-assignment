import math

def area_of_circle(r):
   if r<0:
       print("Radius cannot be negetive")
   return math.pi *r**2

def perimeter_of_rec(l,b):
   if l<0 or b<0:
       print("Length and Breadth cannot be negetive")
   return 2*(l+b)
